<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:id="@+id/loading_overlay"
    android:background="#AAFFFFFF"
    android:clickable="true"
    android:focusableInTouchMode="true"
    android:focusable="true"
    >

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginStart="20dp"
        android:layout_gravity="center"
        android:layout_marginBottom="-10dp"
        android:indeterminateDrawable="@drawable/gear_inverse"
        android:indeterminateTint="@color/tailwind_slate_800"
        android:indeterminateDuration="600"/>

    <ProgressBar
        android:id="@+id/progressBar2"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_gravity="center"
        android:layout_marginEnd="10dp"
        android:indeterminateDrawable="@drawable/gear"
        android:indeterminateTint="@color/tailwind_slate_800"
        android:indeterminateDuration="1000"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:fontFamily="@font/ubuntusans"
        android:text="@string/loading_label"
        android:textAlignment="center"
        android:textColor="@color/tailwind_slate_800"
        android:textSize="18sp"
        android:textStyle="bold" />

</LinearLayout>